<!DOCTYPE html>
<html lang="en">

<%- include('../../partials/admin/head.ejs') %>

    <body>
        <style>
            table.dataTable thead .sorting_asc,
            table.dataTable thead .sorting  {
                background-image: none !important;
                cursor: none !important;
            }

            #search {
                width: 200px !important;
            }

            .form-select {
                display: inline;
                width: 200px !important;
                height: 49px !important;
                
            }

            .form-select:focus {
                border-color: #ff1694;
                outline: 0;
                box-shadow: none; 
            }

        </style>
        <%- include('../../partials/admin/preloader.ejs') %>
            <div id="main-wrapper">
                <%- include('../../partials/admin/nav-header.ejs') %>
                    <%- include('../../partials/admin/chatbox.ejs') %>
                        <%- include('../../partials/admin/header.ejs') %>
                            <%- include('../../partials/admin/sidebar.ejs') %>
                                <div class="content-body">
                                    <div class="container-fluid">
                                        <!-- row -->
                                        <div class="row">
                                            <div class="col-12">
                                                <div class="card">
                                                    <div class="card-header">
                                                        <h4 class="card-title">DIsplay all users</h4>
                                                    </div>
                                                    <div class="card-body">
                                                        <div class="table-responsive">
                                                                <form id="searchForm">
                                                                    <div class="d-flex justify-content-between align-items-center">
                                                                        <select id="role" class="form-select">
                                                                            <option value="">All Roles</option>
                                                                            <option value="1">Guest</option>
                                                                            <option value="2">Organizer</option>
                                                                            <option value="3">Admin</option>
                                                                        </select>
                                                                        <input class="form-control" type="text" id="search" placeholder="Search by name">
                                                                    </div>
                                                                </form>
                                                            <table id="example6" class="display"
                                                                style="min-width: 845px">
                                                                <thead>
                                                                    <tr>
                                                                        <th>ID</th>
                                                                        <th>Avatar</th>
                                                                        <th>Kh_name</th>
                                                                        <th>Eng_name</th>
                                                                        <th>Email</th>
                                                                        <th>Phone</th>
                                                                        <th>Status</th>
                                                                        <th>Address</th>
                                                                        <th>Action</th>
                                                                    </tr>
                                                                </thead>
                                                                <tbody id="usersTableBody">

                                                                </tbody>
                                                            </table>
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                                <%- include('../../partials/admin/footer.ejs') %>
            </div>

            <!-- Required vendors -->
            <script src="/admin/vendor/global/global.min.js"></script>
            <script src="/admin/vendor/chart.js/Chart.bundle.min.js"></script>
            <!-- Apex Chart -->
            <script src="/admin/vendor/apexchart/apexchart.js"></script>

            <!-- Datatable -->
            <script src="/admin/vendor/datatables/js/jquery.dataTables.min.js"></script>
            <script src="/admin/js/plugins-init/datatables.init.js"></script>

            <script src="/admin/vendor/jquery-nice-select/js/jquery.nice-select.min.js"></script>

            <script src="/admin/js/custom.min.js"></script>
            <script src="/admin/js/dlabnav-init.js"></script>
            <script src="/admin/js/demo.js"></script>
            <script src="/admin/js/styleSwitcher.js"></script>

            <script>
                async function fetchUsers(role = '', search = '', page = 1, perPage = 5, sortCol = 'created_at', sortDir = 'asc') {
                    try {
                        const apiUrl = `http://localhost:3000/api/admin/user/display?page=${page}&per_page=${perPage}&sort_col=${sortCol}&sort_dir=${sortDir}&role=${role}&search=${search}`;

                        const response = await fetch(apiUrl);
                        const result = await response.json();

                        if (!result.result) {
                            throw new Error(result.message || 'Failed to fetch users');
                        }

                        displayUsers(result.data);
                    } catch (error) {
                        console.error('Error fetching users:', error);
                    }
                }

                function displayUsers(users) {
                    const tableBody = document.getElementById('usersTableBody');
                    tableBody.innerHTML = '';

                    users.forEach(user => {
                        const row = `
                            <tr>
                                <td>${user.id}</td>
                                <td><img class="rounded-circle" width="35" height="35" src="/uploads/${user.avatar}"></td>
                                <td>${user.kh_name}</td>
                                <td>${user.eng_name}</td>
                                <td>${user.email}</td>
                                <td>${user.phone}</td>
                                <td>
                                    <span class="badge ${user.status === 1 ? 'badge-success' : 'badge-danger'}">
                                        ${user.status === 1 ? 'Active' : 'Inactive'}
                                    </span>
                                </td>
                                <td>${user.address}</td>
                                <td>
                                    <div class="dropdown ms-auto text-end">
                                        <div class="btn-link" data-bs-toggle="dropdown">
                                            <svg width="24px" height="24px" viewbox="0 0 24 24" version="1.1">
                                                <g stroke="none" stroke-width="1" fill="none" fill-rule="evenodd">
                                                    <rect x="0" y="0" width="24" height="24"></rect>
                                                    <circle fill="#000000" cx="5" cy="12" r="2"></circle>
                                                    <circle fill="#000000" cx="12" cy="12" r="2"></circle>
                                                    <circle fill="#000000" cx="19" cy="12" r="2"></circle>
                                                </g>
                                            </svg>
                                        </div>
                                        <div class="dropdown-menu dropdown-menu-end">
                                            <a class="dropdown-item" href="#">View details</a>
                                            <a class="dropdown-item" href="#">Edit User</a>
                                            <a class="dropdown-item" href="#">Remove User</a>
                                        </div>
                                    </div>
                                </td>
                            </tr>
                        `;
                        tableBody.innerHTML += row;
                    });
                }

                function getRoleName(role) {
                    switch (role) {
                        case 1: return 'Guest';
                        case 2: return 'Organizer';
                        case 3: return 'Admin';
                        default: return 'Unknown';
                    }
                }

                document.getElementById('search').addEventListener('keyup', function () {
                    const role = document.getElementById('role').value;
                    const search = this.value;
                    fetchUsers(role, search);
                });

                document.getElementById('role').addEventListener('change', function () {
                    const role = this.value;
                    const search = document.getElementById('search').value;
                    fetchUsers(role, search);
                });

                document.getElementById('example6_paginate').style.display = "none";

                fetchUsers();
            </script>


    </body>

</html>